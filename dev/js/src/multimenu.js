define(
  ['menu'],
  function (menuClass) {
    return {
      create : function (hmm) {
        var obj.create(menuClass)
            .upgradeTo(this)
            ._init(hmm);
      },
      chosen : function () {
      }
    }
  }
);
